name: Deploy to Dev

on:
  push:
    branches:
      - main

env:
  PROJECT_ID: recon-484118
  REGION: us-central1
  WORKLOAD_IDENTITY_PROVIDER: projects/273165967255/locations/global/workloadIdentityPools/github-pool/providers/github-provider
  SERVICE_ACCOUNT: github-actions@recon-484118.iam.gserviceaccount.com

jobs:
  deploy-api:
    name: Deploy API to Dev
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Build and push API image
        run: |
          gcloud builds submit . \
            --config=.github/cloudbuild-api.yaml \
            --substitutions=_IMAGE_NAME=recon-api-dev,COMMIT_SHA=${{ github.sha }} \
            --suppress-logs

      - name: Deploy API to Cloud Run
        run: |
          gcloud run deploy recon-api-dev \
            --image gcr.io/${{ env.PROJECT_ID }}/recon-api-dev:${{ github.sha }} \
            --region ${{ env.REGION }} \
            --platform managed \
            --allow-unauthenticated \
            --port 8080 \
            --set-secrets=FMP_API_KEY=fmp-api-key:latest,POLYGON_API_KEY=polygon-api-key:latest \
            --memory 512Mi \
            --cpu 1 \
            --min-instances 0 \
            --max-instances 2

  deploy-web:
    name: Deploy Web to Dev
    runs-on: ubuntu-latest
    needs: deploy-api
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ env.SERVICE_ACCOUNT }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Build and push Web image
        run: |
          gcloud builds submit . \
            --config=.github/cloudbuild-web.yaml \
            --substitutions=_IMAGE_NAME=recon-web-dev,_API_URL=https://recon-api-dev-273165967255.us-central1.run.app,COMMIT_SHA=${{ github.sha }} \
            --suppress-logs

      - name: Deploy Web to Cloud Run
        run: |
          gcloud run deploy recon-web-dev \
            --image gcr.io/${{ env.PROJECT_ID }}/recon-web-dev:${{ github.sha }} \
            --region ${{ env.REGION }} \
            --platform managed \
            --allow-unauthenticated \
            --port 3000 \
            --memory 512Mi \
            --cpu 1 \
            --min-instances 0 \
            --max-instances 2
