# Server Configuration
PORT=8080
ENV=development

# API Keys (Required)
FMP_API_KEY=your_fmp_api_key
POLYGON_API_KEY=your_polygon_api_key

# Database (Optional - runs without cache if not set)
# DATABASE_URL=postgres://user:pass@localhost:5432/recon

# CORS (comma-separated list, defaults to localhost:3000)
# ALLOWED_ORIGINS=http://localhost:3000,https://recon.example.com

# CruxAI Configuration (Optional - AI-powered insights)
# CRUX_AI_ENABLED=true
# GCP_PROJECT_ID=your-gcp-project-id
# GCP_LOCATION=us-central1
# CRUX_AI_MODEL=gemini-2.0-flash-001
# CRUX_AI_TEMPERATURE=0.7
# CRUX_AI_MAX_TOKENS=300

# GCP Authentication (local dev only - Cloud Run uses workload identity)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json
